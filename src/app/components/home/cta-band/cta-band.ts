import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-cta-band',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './cta-band.html',
  styleUrls: ['./cta-band.css']
})
export class CtaBandComponent {}
